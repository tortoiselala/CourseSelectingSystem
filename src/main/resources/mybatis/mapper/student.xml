<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.tortoise.dao.StudentDao">
    <select id="getPasswordById" resultType="string">
        select password from student
        where id = #{id}
    </select>

    <update id="updateLoginDateById">
        update ignore student
        set login_date=#{date}
        where id = #{id}
    </update>

    <select id="getStudentById" resultType="cn.tortoise.entity.Student">
        select id, sex, name, grade, password, major_id, school_id, login_date
        from student
        where id=#{id}
    </select>

    <insert id="selectCourse">
        insert ignore into selected_course (student_id, course_id)
        values (#{studentId}, #{courseId})
    </insert>

    <select id="getAllSelectedCourseById" resultType="cn.tortoise.entity.SelectedCourse">
        select student_id, course_id, score
        from selected_course
        where student_id = #{courseId}
    </select>
</mapper>
